<!-- Generated by tools/api-reference. Do not edit by hand. -->

# HPD.ML.Backends.Pjrt.PjrtClientInfo

- Package: `HPD-ML-Backends` `0.5.0`
- Assembly: `HPD.ML.Backends`
- Namespace: `HPD.ML.Backends.Pjrt`
- Kind: `class`

Basic runtime metadata from a created PJRT client.

## Declaration

```csharp
public sealed class PjrtClientInfo : System.IEquatable<HPD.ML.Backends.Pjrt.PjrtClientInfo>
```

## Constructors

### PjrtClientInfo

```csharp
public PjrtClientInfo()
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Properties

### DeviceCount

```csharp
public int DeviceCount { get; init; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### PlatformName

```csharp
public string PlatformName { get; init; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### PlatformVersion

```csharp
public string PlatformVersion { get; init; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Methods

### Equals

```csharp
public bool Equals(HPD.ML.Backends.Pjrt.PjrtClientInfo? other)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Equals

```csharp
public virtual bool Equals(object? obj)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### GetHashCode

```csharp
public virtual int GetHashCode()
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### ToString

```csharp
public virtual string ToString()
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### operator !=

```csharp
public static bool operator !=(HPD.ML.Backends.Pjrt.PjrtClientInfo? left, HPD.ML.Backends.Pjrt.PjrtClientInfo? right)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### operator ==

```csharp
public static bool operator ==(HPD.ML.Backends.Pjrt.PjrtClientInfo? left, HPD.ML.Backends.Pjrt.PjrtClientInfo? right)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.
