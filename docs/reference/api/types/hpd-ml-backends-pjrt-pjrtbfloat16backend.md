<!-- Generated by tools/api-reference. Do not edit by hand. -->

# HPD.ML.Backends.Pjrt.PjrtBFloat16Backend

- Package: `HPD-ML-Backends` `0.5.0`
- Assembly: `HPD.ML.Backends`
- Namespace: `HPD.ML.Backends.Pjrt`
- Kind: `class`

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Declaration

```csharp
public sealed class PjrtBFloat16Backend : Helium.Hardware.IExecutionBackend<HPD.ML.Backends.Pjrt.BFloat16>, System.IDisposable
```

## Properties

### CachedExecutableCount

```csharp
public int CachedExecutableCount { get; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Methods

### Add

```csharp
public HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor Add(HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor a, HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Broadcast

```csharp
public HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor Broadcast(HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor scalar, int rows, int cols)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Concatenate

```csharp
public HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor Concatenate(HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor a, HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor b, int axis)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Create

```csharp
public static HPD.ML.Backends.Pjrt.PjrtBFloat16Backend Create(HPD.ML.Backends.Pjrt.PjrtPluginResolverOptions? options = null)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### CreateMatrix

```csharp
public HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor CreateMatrix(int rows, int cols, System.ReadOnlySpan<HPD.ML.Backends.Pjrt.BFloat16> data = null)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### CreateMatrix

```csharp
public HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor CreateMatrix(int rows, int cols, System.ReadOnlySpan<float> data)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Dispose

```csharp
public void Dispose()
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### MatMul

```csharp
public HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor MatMul(HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor a, HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Multiply

```csharp
public HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor Multiply(HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor a, HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Negate

```csharp
public HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor Negate(HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Reshape

```csharp
public HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor Reshape(HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor value, int rows, int cols)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Scale

```csharp
public HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor Scale(HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor value, HPD.ML.Backends.Pjrt.BFloat16 scalar)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Slice

```csharp
public HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor Slice(HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor value, int startRow, int startCol, int rowCount, int colCount)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Subtract

```csharp
public HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor Subtract(HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor a, HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Sum

```csharp
public HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor Sum(HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Transpose

```csharp
public HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor Transpose(HPD.ML.Backends.Pjrt.PjrtBFloat16Tensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### TryCreate

```csharp
public static bool TryCreate(out HPD.ML.Backends.Pjrt.PjrtBFloat16Backend? backend, HPD.ML.Backends.Pjrt.PjrtPluginResolverOptions? options = null)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.
