<!-- Generated by tools/api-reference. Do not edit by hand. -->

# HPD.ML.Backends.Pjrt.PjrtPluginInfo

- Package: `HPD-ML-Backends` `0.5.0`
- Assembly: `HPD.ML.Backends`
- Namespace: `HPD.ML.Backends.Pjrt`
- Kind: `class`

Basic metadata read from a PJRT plugin without creating public tensor objects.

## Declaration

```csharp
public sealed class PjrtPluginInfo : System.IEquatable<HPD.ML.Backends.Pjrt.PjrtPluginInfo>
```

## Constructors

### PjrtPluginInfo

```csharp
public PjrtPluginInfo()
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Properties

### ApiStructSize

```csharp
public nuint ApiStructSize { get; init; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### ApiVersion

```csharp
public HPD.ML.Backends.Pjrt.PjrtApiVersion ApiVersion { get; init; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### LibraryPath

```csharp
public string LibraryPath { get; init; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Methods

### Equals

```csharp
public bool Equals(HPD.ML.Backends.Pjrt.PjrtPluginInfo? other)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Equals

```csharp
public virtual bool Equals(object? obj)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### GetHashCode

```csharp
public virtual int GetHashCode()
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### ToString

```csharp
public virtual string ToString()
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### operator !=

```csharp
public static bool operator !=(HPD.ML.Backends.Pjrt.PjrtPluginInfo? left, HPD.ML.Backends.Pjrt.PjrtPluginInfo? right)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### operator ==

```csharp
public static bool operator ==(HPD.ML.Backends.Pjrt.PjrtPluginInfo? left, HPD.ML.Backends.Pjrt.PjrtPluginInfo? right)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.
