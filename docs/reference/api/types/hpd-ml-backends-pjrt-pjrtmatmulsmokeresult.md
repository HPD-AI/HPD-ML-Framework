<!-- Generated by tools/api-reference. Do not edit by hand. -->

# HPD.ML.Backends.Pjrt.PjrtMatMulSmokeResult

- Package: `HPD-ML-Backends` `0.5.0`
- Assembly: `HPD.ML.Backends`
- Namespace: `HPD.ML.Backends.Pjrt`
- Kind: `class`

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Declaration

```csharp
public sealed class PjrtMatMulSmokeResult : System.IEquatable<HPD.ML.Backends.Pjrt.PjrtMatMulSmokeResult>
```

## Constructors

### PjrtMatMulSmokeResult

```csharp
public PjrtMatMulSmokeResult()
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Properties

### BackendDisposed

```csharp
public bool BackendDisposed { get; init; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### CachedExecutableCount

```csharp
public int CachedExecutableCount { get; init; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### ClientInfo

```csharp
public HPD.ML.Backends.Pjrt.PjrtClientInfo ClientInfo { get; init; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Expected

```csharp
public float[] Expected { get; init; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Output

```csharp
public float[] Output { get; init; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### OutputMatchesExpected

```csharp
public bool OutputMatchesExpected { get; init; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### PluginInfo

```csharp
public HPD.ML.Backends.Pjrt.PjrtPluginInfo PluginInfo { get; init; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Resolution

```csharp
public HPD.ML.Backends.Pjrt.PjrtPluginResolution Resolution { get; init; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Methods

### Equals

```csharp
public bool Equals(HPD.ML.Backends.Pjrt.PjrtMatMulSmokeResult? other)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Equals

```csharp
public virtual bool Equals(object? obj)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### GetHashCode

```csharp
public virtual int GetHashCode()
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### ToString

```csharp
public virtual string ToString()
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### operator !=

```csharp
public static bool operator !=(HPD.ML.Backends.Pjrt.PjrtMatMulSmokeResult? left, HPD.ML.Backends.Pjrt.PjrtMatMulSmokeResult? right)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### operator ==

```csharp
public static bool operator ==(HPD.ML.Backends.Pjrt.PjrtMatMulSmokeResult? left, HPD.ML.Backends.Pjrt.PjrtMatMulSmokeResult? right)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.
