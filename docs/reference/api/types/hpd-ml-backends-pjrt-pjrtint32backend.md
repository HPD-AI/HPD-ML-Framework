<!-- Generated by tools/api-reference. Do not edit by hand. -->

# HPD.ML.Backends.Pjrt.PjrtInt32Backend

- Package: `HPD-ML-Backends` `0.5.0`
- Assembly: `HPD.ML.Backends`
- Namespace: `HPD.ML.Backends.Pjrt`
- Kind: `class`

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Declaration

```csharp
public sealed class PjrtInt32Backend : Helium.Hardware.IExecutionBackend<int>, System.IDisposable
```

## Properties

### CachedExecutableCount

```csharp
public int CachedExecutableCount { get; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Methods

### Add

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt32Tensor Add(HPD.ML.Backends.Pjrt.PjrtInt32Tensor a, HPD.ML.Backends.Pjrt.PjrtInt32Tensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Broadcast

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt32Tensor Broadcast(HPD.ML.Backends.Pjrt.PjrtInt32Tensor scalar, int rows, int cols)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Concatenate

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt32Tensor Concatenate(HPD.ML.Backends.Pjrt.PjrtInt32Tensor a, HPD.ML.Backends.Pjrt.PjrtInt32Tensor b, int axis)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Create

```csharp
public static HPD.ML.Backends.Pjrt.PjrtInt32Backend Create(HPD.ML.Backends.Pjrt.PjrtPluginResolverOptions? options = null)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### CreateMatrix

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt32Tensor CreateMatrix(int rows, int cols, System.ReadOnlySpan<int> data = null)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Dispose

```csharp
public void Dispose()
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### MatMul

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt32Tensor MatMul(HPD.ML.Backends.Pjrt.PjrtInt32Tensor a, HPD.ML.Backends.Pjrt.PjrtInt32Tensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Multiply

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt32Tensor Multiply(HPD.ML.Backends.Pjrt.PjrtInt32Tensor a, HPD.ML.Backends.Pjrt.PjrtInt32Tensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Negate

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt32Tensor Negate(HPD.ML.Backends.Pjrt.PjrtInt32Tensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Reshape

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt32Tensor Reshape(HPD.ML.Backends.Pjrt.PjrtInt32Tensor value, int rows, int cols)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Scale

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt32Tensor Scale(HPD.ML.Backends.Pjrt.PjrtInt32Tensor value, int scalar)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Slice

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt32Tensor Slice(HPD.ML.Backends.Pjrt.PjrtInt32Tensor value, int startRow, int startCol, int rowCount, int colCount)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Subtract

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt32Tensor Subtract(HPD.ML.Backends.Pjrt.PjrtInt32Tensor a, HPD.ML.Backends.Pjrt.PjrtInt32Tensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Sum

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt32Tensor Sum(HPD.ML.Backends.Pjrt.PjrtInt32Tensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Transpose

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt32Tensor Transpose(HPD.ML.Backends.Pjrt.PjrtInt32Tensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### TryCreate

```csharp
public static bool TryCreate(out HPD.ML.Backends.Pjrt.PjrtInt32Backend? backend, HPD.ML.Backends.Pjrt.PjrtPluginResolverOptions? options = null)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.
