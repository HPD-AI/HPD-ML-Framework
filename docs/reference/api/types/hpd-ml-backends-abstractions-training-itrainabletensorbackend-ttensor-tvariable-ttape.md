<!-- Generated by tools/api-reference. Do not edit by hand. -->

# HPD.ML.Backends.Abstractions.Training.ITrainableTensorBackend&lt;TTensor, TVariable, TTape&gt;

- Package: `HPD-ML-Backends-Abstractions` `0.5.0`
- Assembly: `HPD.ML.Backends.Abstractions`
- Namespace: `HPD.ML.Backends.Abstractions.Training`
- Kind: `interface`

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

## Declaration

```csharp
public interface ITrainableTensorBackend<TTensor, TVariable, TTape>
```

## Methods

### Add

```csharp
public abstract TVariable Add(TTape tape, TVariable left, TVariable right)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### BroadcastTo

```csharp
public abstract TVariable BroadcastTo(TTape tape, TVariable value, int rows, int cols)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### Cols

```csharp
public abstract int Cols(TTensor value)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### Cols

```csharp
public abstract int Cols(TVariable value)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### CreateMatrix

```csharp
public abstract TTensor CreateMatrix(int rows, int cols, System.ReadOnlySpan<float> data = null)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### CreateTape

```csharp
public abstract TTape CreateTape()
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### Gradient

```csharp
public abstract TTensor Gradient(TTape tape, TVariable output, TVariable input)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### MatMul

```csharp
public abstract TVariable MatMul(TTape tape, TVariable left, TVariable right)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### Mean

```csharp
public abstract TVariable Mean(TTape tape, TVariable value)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### Multiply

```csharp
public abstract TVariable Multiply(TTape tape, TVariable left, TVariable right)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### ReadScalar

```csharp
public abstract float ReadScalar(TTensor value)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### Rows

```csharp
public abstract int Rows(TTensor value)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### Rows

```csharp
public abstract int Rows(TVariable value)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### Scale

```csharp
public abstract TTensor Scale(TTensor value, float scalar)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### Scale

```csharp
public abstract TVariable Scale(TTape tape, TVariable value, float scalar)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### Subtract

```csharp
public abstract TTensor Subtract(TTensor left, TTensor right)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### Subtract

```csharp
public abstract TVariable Subtract(TTape tape, TVariable left, TVariable right)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### ToArray

```csharp
public abstract float[] ToArray(TTensor value)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### Update

```csharp
public abstract void Update(TTensor value, System.ReadOnlySpan<float> data)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### Value

```csharp
public abstract TTensor Value(TVariable variable)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.

### Watch

```csharp
public abstract TVariable Watch(TTape tape, TTensor value)
```

Documentation: Not provided in `HPD-ML-Backends-Abstractions` `0.5.0`.
