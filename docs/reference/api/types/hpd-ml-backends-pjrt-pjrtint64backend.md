<!-- Generated by tools/api-reference. Do not edit by hand. -->

# HPD.ML.Backends.Pjrt.PjrtInt64Backend

- Package: `HPD-ML-Backends` `0.5.0`
- Assembly: `HPD.ML.Backends`
- Namespace: `HPD.ML.Backends.Pjrt`
- Kind: `class`

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Declaration

```csharp
public sealed class PjrtInt64Backend : Helium.Hardware.IExecutionBackend<long>, System.IDisposable
```

## Properties

### CachedExecutableCount

```csharp
public int CachedExecutableCount { get; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Methods

### Add

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt64Tensor Add(HPD.ML.Backends.Pjrt.PjrtInt64Tensor a, HPD.ML.Backends.Pjrt.PjrtInt64Tensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Broadcast

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt64Tensor Broadcast(HPD.ML.Backends.Pjrt.PjrtInt64Tensor scalar, int rows, int cols)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Concatenate

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt64Tensor Concatenate(HPD.ML.Backends.Pjrt.PjrtInt64Tensor a, HPD.ML.Backends.Pjrt.PjrtInt64Tensor b, int axis)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Create

```csharp
public static HPD.ML.Backends.Pjrt.PjrtInt64Backend Create(HPD.ML.Backends.Pjrt.PjrtPluginResolverOptions? options = null)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### CreateMatrix

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt64Tensor CreateMatrix(int rows, int cols, System.ReadOnlySpan<long> data = null)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Dispose

```csharp
public void Dispose()
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### MatMul

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt64Tensor MatMul(HPD.ML.Backends.Pjrt.PjrtInt64Tensor a, HPD.ML.Backends.Pjrt.PjrtInt64Tensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Multiply

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt64Tensor Multiply(HPD.ML.Backends.Pjrt.PjrtInt64Tensor a, HPD.ML.Backends.Pjrt.PjrtInt64Tensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Negate

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt64Tensor Negate(HPD.ML.Backends.Pjrt.PjrtInt64Tensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Reshape

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt64Tensor Reshape(HPD.ML.Backends.Pjrt.PjrtInt64Tensor value, int rows, int cols)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Scale

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt64Tensor Scale(HPD.ML.Backends.Pjrt.PjrtInt64Tensor value, long scalar)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Slice

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt64Tensor Slice(HPD.ML.Backends.Pjrt.PjrtInt64Tensor value, int startRow, int startCol, int rowCount, int colCount)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Subtract

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt64Tensor Subtract(HPD.ML.Backends.Pjrt.PjrtInt64Tensor a, HPD.ML.Backends.Pjrt.PjrtInt64Tensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Sum

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt64Tensor Sum(HPD.ML.Backends.Pjrt.PjrtInt64Tensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Transpose

```csharp
public HPD.ML.Backends.Pjrt.PjrtInt64Tensor Transpose(HPD.ML.Backends.Pjrt.PjrtInt64Tensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### TryCreate

```csharp
public static bool TryCreate(out HPD.ML.Backends.Pjrt.PjrtInt64Backend? backend, HPD.ML.Backends.Pjrt.PjrtPluginResolverOptions? options = null)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.
