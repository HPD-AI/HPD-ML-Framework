<!-- Generated by tools/api-reference. Do not edit by hand. -->

# HPD.ML.Backends.Pjrt.PjrtTensorTape

- Package: `HPD-ML-Backends` `0.5.0`
- Assembly: `HPD.ML.Backends`
- Namespace: `HPD.ML.Backends.Pjrt`
- Kind: `class`

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Declaration

```csharp
public sealed class PjrtTensorTape : System.IDisposable
```

## Constructors

### PjrtTensorTape

```csharp
public PjrtTensorTape(HPD.ML.Backends.Pjrt.PjrtFloatBackend backend)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Methods

### Add

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Add(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar left, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar right)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Broadcast

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Broadcast(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar scalar, int rows, int cols)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### BroadcastTo

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar BroadcastTo(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar value, int rows, int cols)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Concatenate

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Concatenate(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar left, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar right, int axis)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Constant

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Constant(int rows, int cols, System.ReadOnlySpan<float> values)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Dispose

```csharp
public void Dispose()
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Gradient

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor Gradient(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar output, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar input)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Gradients

```csharp
public System.Collections.Generic.IReadOnlyDictionary<int, HPD.ML.Backends.Pjrt.PjrtFloatTensor> Gradients(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar output)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### MatMul

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar MatMul(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar left, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar right)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Mean

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Mean(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Multiply

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Multiply(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar left, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar right)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Negate

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Negate(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### ReLU

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar ReLU(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Reshape

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Reshape(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar value, int rows, int cols)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Scale

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Scale(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar value, float scalar)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Slice

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Slice(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar value, int startRow, int startCol, int rowCount, int colCount)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Subtract

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Subtract(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar left, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar right)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Sum

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Sum(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Transpose

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Transpose(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Watch

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Watch(HPD.ML.Backends.Pjrt.PjrtFloatTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.
