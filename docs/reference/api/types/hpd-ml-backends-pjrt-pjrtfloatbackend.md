<!-- Generated by tools/api-reference. Do not edit by hand. -->

# HPD.ML.Backends.Pjrt.PjrtFloatBackend

- Package: `HPD-ML-Backends` `0.5.0`
- Assembly: `HPD.ML.Backends`
- Namespace: `HPD.ML.Backends.Pjrt`
- Kind: `class`

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Declaration

```csharp
public sealed class PjrtFloatBackend : Helium.Hardware.IExecutionBackend<float>, System.IDisposable
```

## Properties

### CachedExecutableCount

```csharp
public int CachedExecutableCount { get; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### ClientInfo

```csharp
public HPD.ML.Backends.Pjrt.PjrtClientInfo ClientInfo { get; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### PluginInfo

```csharp
public HPD.ML.Backends.Pjrt.PjrtPluginInfo PluginInfo { get; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Methods

### Add

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor Add(HPD.ML.Backends.Pjrt.PjrtFloatTensor a, HPD.ML.Backends.Pjrt.PjrtFloatTensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Broadcast

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor Broadcast(HPD.ML.Backends.Pjrt.PjrtFloatTensor scalar, int rows, int cols)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### BroadcastTo

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor BroadcastTo(HPD.ML.Backends.Pjrt.PjrtFloatTensor value, int rows, int cols)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Concatenate

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor Concatenate(HPD.ML.Backends.Pjrt.PjrtFloatTensor a, HPD.ML.Backends.Pjrt.PjrtFloatTensor b, int axis)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Create

```csharp
public static HPD.ML.Backends.Pjrt.PjrtFloatBackend Create(HPD.ML.Backends.Pjrt.PjrtPluginResolverOptions? options = null)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### CreateMatrix

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor CreateMatrix(int rows, int cols, System.ReadOnlySpan<float> data = null)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Dispose

```csharp
public void Dispose()
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### IsAvailable

```csharp
public static bool IsAvailable(HPD.ML.Backends.Pjrt.PjrtPluginResolverOptions? options = null)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### LinearSolve

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor LinearSolve(HPD.ML.Backends.Pjrt.PjrtFloatTensor matrix, HPD.ML.Backends.Pjrt.PjrtFloatTensor rightHandSide)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### MatMul

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor MatMul(HPD.ML.Backends.Pjrt.PjrtFloatTensor a, HPD.ML.Backends.Pjrt.PjrtFloatTensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### MatrixInverse

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor MatrixInverse(HPD.ML.Backends.Pjrt.PjrtFloatTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Mean

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor Mean(HPD.ML.Backends.Pjrt.PjrtFloatTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Multiply

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor Multiply(HPD.ML.Backends.Pjrt.PjrtFloatTensor a, HPD.ML.Backends.Pjrt.PjrtFloatTensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Negate

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor Negate(HPD.ML.Backends.Pjrt.PjrtFloatTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Norm

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor Norm(HPD.ML.Backends.Pjrt.PjrtFloatTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### ReLU

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor ReLU(HPD.ML.Backends.Pjrt.PjrtFloatTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Reshape

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor Reshape(HPD.ML.Backends.Pjrt.PjrtFloatTensor value, int rows, int cols)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Scale

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor Scale(HPD.ML.Backends.Pjrt.PjrtFloatTensor value, float scalar)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Slice

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor Slice(HPD.ML.Backends.Pjrt.PjrtFloatTensor value, int startRow, int startCol, int rowCount, int colCount)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Subtract

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor Subtract(HPD.ML.Backends.Pjrt.PjrtFloatTensor a, HPD.ML.Backends.Pjrt.PjrtFloatTensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Sum

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor Sum(HPD.ML.Backends.Pjrt.PjrtFloatTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Transpose

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor Transpose(HPD.ML.Backends.Pjrt.PjrtFloatTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### TriangularSolve

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor TriangularSolve(HPD.ML.Backends.Pjrt.PjrtFloatTensor matrix, HPD.ML.Backends.Pjrt.PjrtFloatTensor rightHandSide, bool lower, bool unitDiagonal = false)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### TryCreate

```csharp
public static bool TryCreate(out HPD.ML.Backends.Pjrt.PjrtFloatBackend? backend, HPD.ML.Backends.Pjrt.PjrtPluginResolverOptions? options = null)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.
