<!-- Generated by tools/api-reference. Do not edit by hand. -->

# HPD.ML.Backends.Pjrt.PjrtDoubleBackend

- Package: `HPD-ML-Backends` `0.5.0`
- Assembly: `HPD.ML.Backends`
- Namespace: `HPD.ML.Backends.Pjrt`
- Kind: `class`

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Declaration

```csharp
public sealed class PjrtDoubleBackend : Helium.Hardware.IExecutionBackend<double>, System.IDisposable
```

## Properties

### CachedExecutableCount

```csharp
public int CachedExecutableCount { get; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### ClientInfo

```csharp
public HPD.ML.Backends.Pjrt.PjrtClientInfo ClientInfo { get; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### PluginInfo

```csharp
public HPD.ML.Backends.Pjrt.PjrtPluginInfo PluginInfo { get; }
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Methods

### Add

```csharp
public HPD.ML.Backends.Pjrt.PjrtDoubleTensor Add(HPD.ML.Backends.Pjrt.PjrtDoubleTensor a, HPD.ML.Backends.Pjrt.PjrtDoubleTensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Broadcast

```csharp
public HPD.ML.Backends.Pjrt.PjrtDoubleTensor Broadcast(HPD.ML.Backends.Pjrt.PjrtDoubleTensor scalar, int rows, int cols)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Concatenate

```csharp
public HPD.ML.Backends.Pjrt.PjrtDoubleTensor Concatenate(HPD.ML.Backends.Pjrt.PjrtDoubleTensor a, HPD.ML.Backends.Pjrt.PjrtDoubleTensor b, int axis)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Create

```csharp
public static HPD.ML.Backends.Pjrt.PjrtDoubleBackend Create(HPD.ML.Backends.Pjrt.PjrtPluginResolverOptions? options = null)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### CreateMatrix

```csharp
public HPD.ML.Backends.Pjrt.PjrtDoubleTensor CreateMatrix(int rows, int cols, System.ReadOnlySpan<double> data = null)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Dispose

```csharp
public void Dispose()
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### IsAvailable

```csharp
public static bool IsAvailable(HPD.ML.Backends.Pjrt.PjrtPluginResolverOptions? options = null)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### LinearSolve

```csharp
public HPD.ML.Backends.Pjrt.PjrtDoubleTensor LinearSolve(HPD.ML.Backends.Pjrt.PjrtDoubleTensor matrix, HPD.ML.Backends.Pjrt.PjrtDoubleTensor rightHandSide)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### MatMul

```csharp
public HPD.ML.Backends.Pjrt.PjrtDoubleTensor MatMul(HPD.ML.Backends.Pjrt.PjrtDoubleTensor a, HPD.ML.Backends.Pjrt.PjrtDoubleTensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### MatrixInverse

```csharp
public HPD.ML.Backends.Pjrt.PjrtDoubleTensor MatrixInverse(HPD.ML.Backends.Pjrt.PjrtDoubleTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Mean

```csharp
public HPD.ML.Backends.Pjrt.PjrtDoubleTensor Mean(HPD.ML.Backends.Pjrt.PjrtDoubleTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Multiply

```csharp
public HPD.ML.Backends.Pjrt.PjrtDoubleTensor Multiply(HPD.ML.Backends.Pjrt.PjrtDoubleTensor a, HPD.ML.Backends.Pjrt.PjrtDoubleTensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Negate

```csharp
public HPD.ML.Backends.Pjrt.PjrtDoubleTensor Negate(HPD.ML.Backends.Pjrt.PjrtDoubleTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Norm

```csharp
public HPD.ML.Backends.Pjrt.PjrtDoubleTensor Norm(HPD.ML.Backends.Pjrt.PjrtDoubleTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Reshape

```csharp
public HPD.ML.Backends.Pjrt.PjrtDoubleTensor Reshape(HPD.ML.Backends.Pjrt.PjrtDoubleTensor value, int rows, int cols)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Scale

```csharp
public HPD.ML.Backends.Pjrt.PjrtDoubleTensor Scale(HPD.ML.Backends.Pjrt.PjrtDoubleTensor value, double scalar)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Slice

```csharp
public HPD.ML.Backends.Pjrt.PjrtDoubleTensor Slice(HPD.ML.Backends.Pjrt.PjrtDoubleTensor value, int startRow, int startCol, int rowCount, int colCount)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Subtract

```csharp
public HPD.ML.Backends.Pjrt.PjrtDoubleTensor Subtract(HPD.ML.Backends.Pjrt.PjrtDoubleTensor a, HPD.ML.Backends.Pjrt.PjrtDoubleTensor b)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Sum

```csharp
public HPD.ML.Backends.Pjrt.PjrtDoubleTensor Sum(HPD.ML.Backends.Pjrt.PjrtDoubleTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Transpose

```csharp
public HPD.ML.Backends.Pjrt.PjrtDoubleTensor Transpose(HPD.ML.Backends.Pjrt.PjrtDoubleTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### TriangularSolve

```csharp
public HPD.ML.Backends.Pjrt.PjrtDoubleTensor TriangularSolve(HPD.ML.Backends.Pjrt.PjrtDoubleTensor matrix, HPD.ML.Backends.Pjrt.PjrtDoubleTensor rightHandSide, bool lower, bool unitDiagonal = false)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### TryCreate

```csharp
public static bool TryCreate(out HPD.ML.Backends.Pjrt.PjrtDoubleBackend? backend, HPD.ML.Backends.Pjrt.PjrtPluginResolverOptions? options = null)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.
