<!-- Generated by tools/api-reference. Do not edit by hand. -->

# HPD.ML.Backends.Pjrt.Training.PjrtTrainableBackend

- Package: `HPD-ML-Backends` `0.5.0`
- Assembly: `HPD.ML.Backends`
- Namespace: `HPD.ML.Backends.Pjrt.Training`
- Kind: `class`

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Declaration

```csharp
public sealed class PjrtTrainableBackend : HPD.ML.Backends.Abstractions.Training.ITrainableTensorBackend<HPD.ML.Backends.Pjrt.PjrtFloatTensor, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar, HPD.ML.Backends.Pjrt.PjrtTensorTape>, HPD.ML.Backends.Abstractions.Training.ITrainableActivationBackend<HPD.ML.Backends.Pjrt.PjrtFloatTensor, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar, HPD.ML.Backends.Pjrt.PjrtTensorTape>
```

## Constructors

### PjrtTrainableBackend

```csharp
public PjrtTrainableBackend(HPD.ML.Backends.Pjrt.PjrtFloatBackend backend)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

## Methods

### Add

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Add(HPD.ML.Backends.Pjrt.PjrtTensorTape tape, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar left, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar right)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### BroadcastTo

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar BroadcastTo(HPD.ML.Backends.Pjrt.PjrtTensorTape tape, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar value, int rows, int cols)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Cols

```csharp
public int Cols(HPD.ML.Backends.Pjrt.PjrtFloatTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Cols

```csharp
public int Cols(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### CreateMatrix

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor CreateMatrix(int rows, int cols, System.ReadOnlySpan<float> data = null)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### CreateTape

```csharp
public HPD.ML.Backends.Pjrt.PjrtTensorTape CreateTape()
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Gradient

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor Gradient(HPD.ML.Backends.Pjrt.PjrtTensorTape tape, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar output, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar input)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### MatMul

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar MatMul(HPD.ML.Backends.Pjrt.PjrtTensorTape tape, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar left, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar right)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Mean

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Mean(HPD.ML.Backends.Pjrt.PjrtTensorTape tape, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Multiply

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Multiply(HPD.ML.Backends.Pjrt.PjrtTensorTape tape, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar left, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar right)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### ReLU

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar ReLU(HPD.ML.Backends.Pjrt.PjrtTensorTape tape, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### ReadScalar

```csharp
public float ReadScalar(HPD.ML.Backends.Pjrt.PjrtFloatTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Rows

```csharp
public int Rows(HPD.ML.Backends.Pjrt.PjrtFloatTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Rows

```csharp
public int Rows(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Scale

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor Scale(HPD.ML.Backends.Pjrt.PjrtFloatTensor value, float scalar)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Scale

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Scale(HPD.ML.Backends.Pjrt.PjrtTensorTape tape, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar value, float scalar)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Subtract

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor Subtract(HPD.ML.Backends.Pjrt.PjrtFloatTensor left, HPD.ML.Backends.Pjrt.PjrtFloatTensor right)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Subtract

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Subtract(HPD.ML.Backends.Pjrt.PjrtTensorTape tape, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar left, HPD.ML.Backends.Pjrt.PjrtFloatTensorVar right)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### ToArray

```csharp
public float[] ToArray(HPD.ML.Backends.Pjrt.PjrtFloatTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Update

```csharp
public void Update(HPD.ML.Backends.Pjrt.PjrtFloatTensor value, System.ReadOnlySpan<float> data)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Value

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensor Value(HPD.ML.Backends.Pjrt.PjrtFloatTensorVar variable)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.

### Watch

```csharp
public HPD.ML.Backends.Pjrt.PjrtFloatTensorVar Watch(HPD.ML.Backends.Pjrt.PjrtTensorTape tape, HPD.ML.Backends.Pjrt.PjrtFloatTensor value)
```

Documentation: Not provided in `HPD-ML-Backends` `0.5.0`.
